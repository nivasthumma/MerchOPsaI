# A Node too old for the toolchain should fail at `npm install` with one line
# naming the version, not thirty seconds into `npm test` with a syntax error
# from inside a dependency. Vitest 5 requires >= 22.12; `engines` in
# package.json only warns without this.
engine-strict=true
